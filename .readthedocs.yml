version: 2

sphinx:
  configuration: docs/source/conf.py

formats: all

build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-4.10"

conda:
  environment: environment-dev.yml

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
